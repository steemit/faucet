import { jest } from '@jest/globals';
// import apiHandlers from './apiHandlers.js';
// import mockDb from '../helpers/database.js';
// import mockServices from '../helpers/services.js';

describe('request email verification step', () => {
  beforeEach(() => {
    jest.resetModules();
  });

  it('demo test', () => {
    expect(1).toEqual(1);
  });
});
